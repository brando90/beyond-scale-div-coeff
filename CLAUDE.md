# Project: beyond-scale-div-coeff

## Agent config loading (do this first)

1. **Check if `~/agents-config/` exists locally.** If yes, read `~/agents-config/INDEX_RULES.md` directly.
2. **If not (e.g., cloud sandbox, phone session),** use `WebFetch` to read `https://raw.githubusercontent.com/brando90/agents-config/main/INDEX_RULES.md` and load its contents into your active context. Then fetch any other files referenced by INDEX_RULES.md as needed (machine configs, workflow docs).
3. **Follow all rules in INDEX_RULES.md** for the rest of the session — including PR format, QA workflows, TLDR endings, commit discipline, etc.

## Project-level docs

Read the project-level agent index for project-specific docs:
- `~/beyond-scale-div-coeff/docs/agent-docs/INDEX.md`
